# 会話のスケッチ 検証

2026-09-13。GPTLiveVoiceTest専用。マイクを開始しないテスト。

- Python全29テストPASS。引用根拠の検証、未知/旧IDと連打の拒否、タップを発言と分離、WebSocketのタップ受付→Liveへの話題指示、非同期候補更新、解析失敗後も音声転送継続、キャンセル時上流切断、従来JSON生成/時間制限の回帰を含む。
- 実GPT-4o APIで「一人で海沿いを散歩、静かに考える時間、写真」という会話から3候補を生成。`LiveApiExample.json`。人物像は断定せず、根拠のある話題を採用。
- Unity Play上で実API候補を表示。既存字幕と波形が左、右は枠なし・下線つきの文字ボタン。クリック用にTMPテキストのraycastTargetを有効化（透明Imageのみではクリックできなかったため修正）。
- 実UIクリックで「趣味の写真撮影」のID `1-2` がSelectedへ届くことを確認。受付応答を模擬して「次は『趣味の写真撮影』の話へ」と選択色を確認。
- EventSystemは1個、既存CanvasのGraphicRaycasterを使用。元シーンアセットへの変更なし。UIは実行時生成。
- 人のマイクを使った会話と、Liveの実際の発話による自然な話題転換は未試聴。候補生成は実API、音声への指示経路はモックWebSocketで確認しており、両者を混同しない。

## 2026-09-13 タップ後の発話再開

話題選択時は instructions.append だけだったため、受理後に commentary.append で反応と質問を促すよう変更。受理IDを照合し、重複・古い選択の受理は無視。終了要求後／closing中は話題の発話を開始しない。マイクや再生は停止しない。

38テスト成功。リレーのモックテストで選択→受理→発話要求→文字起こし、重複受理で1回のみ、音声入力継続を確認。実GPT-Liveに同じ話題指示と発話要求を送り、無音入力でも2.48秒で非無音PCM、選んだ「ポジティブなゲーム」への反応と質問を確認（TopicSpeech-ja.json）。Unity画面の実タップからの通し検証は未実施。
