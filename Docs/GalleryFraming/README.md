# 2026-09-15 キャラ説明文字と回廊の会話構図

- MansionSignalViewの画像下ラベルを日英共通で1.2倍（上限18→21.6、下限12→14.4）。ライバル窓も同じ設定を複製する。
- 回廊の会話中心を部屋内の(-15.8, y, 2)から、植栽より手前の中庭(-3.5, y, 6)へ移動。人物は廊下に沿って並び、中庭側から撮影する。
- 回廊のグループ会話は話者が切り替わっても全員を映す。その他の場所は従来の話者カメラを維持。
- 保存済みScene/Prefabの更新不要。セーブ再開にも同じ配置を適用。

Unityコンパイル成功。Playで日英・全景/話者切替の表示を検証。画像を目視し、植栽や柱に隠れない最終位置へ調整。Verification.txtにキューブ4体の画面内座標・遮蔽物レイの確認結果。通常セーブとAI通信は検証中無効。配布Webビルドの再生成は行っていない。
