
## 2026-09-13 不足表現の除去

最終JSONのlimitationsを除外。人物設定に混入した「不明」「記録できない」などを検出し、確定情報として固定せず補完対象へ戻す。API補完後にも空欄と不足表現を検査し、該当時は補完APIを1回再実行。再失敗は不完全なJSONを保存せずエラー。会話の具体的な値は保持し、創作項目のパスを残す。生成側の再試行に合わせUnity待機を130秒へ。

39テスト成功。無言の実API生成で全キャラクター項目に具体値、limitationsなし、不足表現なしを確認（NoGaps-ja.json）。Unityコンパイルエラーなし。生成タイトル・確認ボタン・演出字幕の日英から「二つの回答／二つの声」を除去し「会話／声」に変更。
